# Taste
- User gives very terse prompts ("try again", "yes") with almost no context; prefers the agent to autonomously investigate the workspace/environment and infer intent before asking clarifying questions. Repeatedly confirmed across many turns. Note: "try again" after a failure means resume from what's already been learned — keep prior findings and adapt the approach (e.g., abandon a dead subagent and do the work directly) rather than restart from scratch. Confidence: 0.9
- User supplies context as bare URLs ("let me give you hint... Read below <link>") — expects the agent to fetch and digest the linked page itself rather than asking what it says. Confidence: 0.8
- Expects work verified end-to-end before being reported done — typecheck/lint, a clean build, then a runtime smoke test (start the server, curl the key routes). Repeated bare "try again" prompts only stopped once output was actually validated, not merely attempted. Confidence: 0.6
- User's GitHub account is `teecash96`; personal repos live at github.com/teecash96/<name> (e.g., the `plow` project at github.com/teecash96/plow). Useful when wiring up remotes or hunting for a missing repo. Confidence: 0.8
- Node toolchain on this Mac: node/npm/npx are installed under `/usr/local/bin` (homebrew's `/opt/homebrew/bin` has no working node symlinks; no nvm/volta/fnm either). Non-interactive shells don't inherit it, so prefix JS commands with `export PATH="/usr/local/bin:$PATH"`. Confidence: 0.8
- User's flagship project is **Plow** (`~/Plow`, github.com/teecash96/plow): a Next.js agent marketplace for BNB Chain built on ERC-8004 identity discovery plus ERC-8183 + x402 hire/payment flow; sibling local dir `~/PlowAgents` holds the seller-side agents (e.g., plowrebalanceprod A2A rebalance seller). The repo uses a recent Next.js where global route types like `LayoutProps` only exist after `npx next typegen` runs — generate types before judging tsc errors. Expect related work to follow this stack. Confidence: 0.8
- User is building Plow for the BNB Chain "Smart Money Era" hackathon; the judging rubric emphasizes four equal-depth categories (rebalancing, grid trading, yield optimisation, health factor monitoring), real-time data quality, and an end-to-end agent hire journey — so audits/status reports should be benchmarked against that rubric. Confidence: 0.8
- Plow is already deployed on Vercel: project `plow` → https://plow-black.vercel.app (last production deploy Aug 18, commit "Initial public release" — same commit as local main, so live code is NOT behind the repo). Verified live on 2026-08-25: the deployed `/agents` page discovers ~100 real ERC-8004 agents off BSC mainnet (IDs 303550–303599+) — deployment healthy, NO redeploy needed despite appearances. The homepage's "Live agent wall" empty state renders **by design**: `/` is a static marketing page that intentionally skips registry fetching at build time while `/agents` is dynamic — don't misread it as a stale/broken build. Known product gaps vs the hackathon rubric (product work, not deploy work): live agents show "Not available" for pricing/heartbeat/metrics (only identity evidence is real); hire flow fails-closed by design (ERC-8183 + x402 env vars blank → discovery-only mode); category assignment relies on a keyword classifier + 5 curated agent IDs so most discovered agents land uncategorised. Next-step candidates floated to user: (a) wire the `plowrebalanceprod` seller in as the first genuinely hireable agent (closes gaps #1+#2), (b) improve category coverage/classification. Vercel CLI auth lives at `~/Library/Application Support/com.vercel.cli/auth.json`; its stored access token can go stale — prefer plain `npx vercel ...` (the CLI refreshes via its refresh token) over hand-extracting the token into `--token` flags. Account team id: `team_5yDZT81Mp2Anl175kjuPMqaN`. Link the repo with `npx vercel link --yes --project plow`. When inspecting auth files, print only truncated/safe fields, never echo secrets — same rule when generating server-held keys: write them programmatically straight into the gitignored `.env.local` and print only the derived public address, never the key itself. Confidence: 0.9
- When the user half-remembers prior work ("i think it has been deployed before, No?"), verify against the machine and account instead of assuming either way: check `.vercel/`, CLI credential dirs, grep repo docs and past-session notes (~/Documents/Codex, ~/Documents/ChatGPT) for URLs, then query the real account via CLI/API. Settle existence before creating duplicate infra or re-deploying. Confidence: 0.7
- The ERC-8183 escrow stack already exists — never deploy new contracts for Plow's hire flow: the seller-side `bnbagent` SDK (at `~/PlowAgents/PlowRebalance/app/agent/.venv/lib/python3.12/site-packages/bnbagent/`) ships official deployed contracts — BSC mainnet commerce `0xea4daa3100a767e86fded867729ae7446476eba6` (+ router/policy) and testnet commerce `0xa206c0517b6371c6638cd9e4a42cc9f02a33b0de`. Canonical ABIs are bundled at `bnbagent/abis/AgenticCommerce.json` and `EvaluatorRouter.json`; the deployed ABI differs from Plow's draft (`jobs()` returns an 11-field struct, `fund()` takes extra args), so map every call against the SDK ABI before wiring the buyer path. Plow's buyer adapter (`src/lib/chain/erc8183-adapter.ts`) has now been updated to the deployed ABI (`getJob(uint256)` returning the full 11-field job struct instead of `jobs()`, `fund(jobId, amount, optParams)` taking an expectedBudget, `JobFunded` gaining `payer`), and `.env.example` + `HIRE_SETUP.md` carry the live addresses plus a known-deployments table. Payment token is $U: `0xcE24439F2D9C6a2289F741120FE202248B666666` (mainnet) / `0xc70b8741b8b07a6d61e54fd4b20f22fa648e5565` (testnet), 18 decimals, EIP-3009 probed because x402 `exact` scheme requires it. Confidence: 0.85
- Keyless on-chain verification workflow for BSC: probe contracts via raw JSON-RPC `eth_call`/`eth_getCode` against public RPCs — `https://bsc.publicnode.com` (mainnet), `https://data-seed-prebsc-2-s2.binance.org:8545` (testnet) — computing 4-byte selectors from the Plow repo's own viem (`node -e "...keccak256(toBytes(sig)).slice(0,10)"`) since pycryptodome is unavailable locally; guess-and-check selector variants only after reading the SDK's ABI surface. Don't trust docs/SDK claims about token capabilities — prove them from bytecode: scan `eth_getCode` output for PUSH4 selector bytes (this exposed that $U has permit/nonces but NO transferWithAuthorization → EIP-2612-only, despite SDK assumptions), detect EIP-1967 proxies and read the impl slot `0x360894...382bbc` to find the real logic, and verify EIP-712 domains by recomputing DOMAIN_SEPARATOR offline and diffing against the onchain call. Confidence: 0.75
 Confidence: 0.75
02/resource/route.ts` (GET issues the 402 challenge with permit2 ATM/$U/payTo=seller, POST verifies + settles via an in-process facilitator using a server-held signer gated by server-only env vars `X402_FACILITATOR_KEY`, `X402_PAYEE_ADDRESS`, `X402_RESOURCE_AMOUNT` — fails-closed like the rest of the hire config). Confidence: 0.85
- Keyless on-chain verification workflow for BSC: probe contracts via raw JSON-RPC `eth_call` against public RPCs — `https://bsc.publicnode.com` (mainnet), `https://data-seed-prebsc-2-s2.binance.org:8545` (testnet) — computing 4-byte selectors from the Plow repo's own viem (`node -e "...keccak256(toBytes(sig)).slice(0,10)"`) since pycryptodome is unavailable locally; guess-and-check selector variants only after reading the SDK's ABI surface. Confidence: 0.7
 Confidence: 0.75
ated by server-only env vars `X402_FACILITATOR_KEY`, `X402_PAYEE_ADDRESS`, `X402_RESOURCE_AMOUNT` — fails-closed like the rest of the hire config). Confidence: 0.85
- Keyless on-chain verification workflow for BSC: probe contracts via raw JSON-RPC `eth_call` against public RPCs — `https://bsc.publicnode.com` (mainnet), `https://data-seed-prebsc-2-s2.binance.org:8545` (testnet) — computing 4-byte selectors from the Plow repo's own viem (`node -e "...keccak256(toBytes(sig)).slice(0,10)"`) since pycryptodome is unavailable locally; guess-and-check selector variants only after reading the SDK's ABI surface. Confidence: 0.7
 Confidence: 0.75
